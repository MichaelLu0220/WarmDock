# React Native / Metro expect a flat (npm-style) node_modules. pnpm's default
# isolated layout hides transitive deps from Metro, so use a hoisted layout for
# the whole workspace. (Web/Desktop/Next/Vite all work fine with hoisting too.)
node-linker=hoisted
